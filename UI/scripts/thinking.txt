
// AT SCRAPING : 
// find the current date (today)
// find when the date will be x weeks / days from now
// determine the day at which web gnome will scrape
    // update the input scrape and saved scrape UI so that they can change the elements

// should I make classes for each of the scrape targets?
    // each class contains:
        // content
        // date made
        // when to scrape again (recurring)

// TO DO:
    // needs to be able to determine the next time that web gnome will scrape AT SCRAPING
    // change the scrapes to instead be folders, which holds:
        // date made
        // date when to scrape again
        // data from most current scrape
        // data from previous to last current scrape