
// AT SCRAPING : 
// find the current date (today)
// find when the date will be x weeks / days from now
// determine the day at which web gnome will scrape
    // update the input scrape and saved scrape UI so that they can change the elements

// should I make classes for each of the scrape targets?
    // each class contains:
        // content
        // date made
        // when to scrape again (recurring)

// TO DO:
    // change the table to have the home website mentioned and if a change has occured
    // change the table to include ALL the scrapes included, not just presenting the home scrape

    // on saved_scrapes.js 
        // make it so that it can compare different txt files
        // make it so that it can compare the dates of each scrape (the scrapes name)
        // you stopped at a point where you are working through comparing an old_scrape to a new_scrape,
            // but are not sure how to store the data of the old scrape